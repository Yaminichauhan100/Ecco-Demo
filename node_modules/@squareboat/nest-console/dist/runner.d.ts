import { CommandObject } from "./interfaces";
export declare class CommandRunner {
    static run(cmd: string): Promise<void>;
    static handle(command: CommandObject | null, args: Record<string, any>): Promise<void>;
    static printOptions(command: CommandObject): void;
}
