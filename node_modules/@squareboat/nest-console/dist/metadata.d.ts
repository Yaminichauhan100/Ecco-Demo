import { CommandObject, CommandMetaOptions, GenericFunction } from "./interfaces";
export declare class CommandMeta {
    private static commands;
    static setCommand(command: string, options: CommandMetaOptions, target: GenericFunction): void;
    static getAllCommands(): Record<string, CommandObject>;
    static getCommand(command: string): CommandObject | null;
    static getTarget(command: string): GenericFunction | null;
}
