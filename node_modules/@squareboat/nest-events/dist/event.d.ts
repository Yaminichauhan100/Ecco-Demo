import "reflect-metadata";
export declare class EmitsEvent {
    private reservedKeyNames;
    emit(): Promise<void>;
    fetchPayload(): Record<string, any>;
    dispatch(eventName: string, queueConnection: Record<string, any> | Record<string, any>[]): Promise<void>;
}
